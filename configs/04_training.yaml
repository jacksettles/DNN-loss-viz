training:
  num_epochs: 10

  criterion:
    name: "cross_entropy_loss"

  optimizer:
    name: "AdamW"
    params:
      lr: 0.001
      weight_decay: 0.0005

  scheduler:
    name: "StepLR"
    params:
      step_size: 5
      gamma: 0.1